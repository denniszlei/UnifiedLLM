# =============================================================================
# UnifiedLLM - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# -----------------------------------------------------------------------------
# ENCRYPTION (REQUIRED)
# -----------------------------------------------------------------------------
# Generate a new encryption key using one of these methods:
#
# Method 1 - Python (if installed):
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# Method 2 - OpenSSL (if Python not available):
#   openssl rand -base64 32
#
# Method 3 - Docker (no local installation needed):
#   docker run --rm python:3.11-slim python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
#
# Method 4 - Online (use a trusted generator):
#   https://generate-random.org/encryption-key-generator (32-byte, Base64)
#
# IMPORTANT: Keep this key secure! If lost, encrypted API keys cannot be recovered.
# The application will NOT start without a valid encryption key.
ENCRYPTION_KEY=your-encryption-key-here

# -----------------------------------------------------------------------------
# GPT-LOAD CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# Authentication key for GPT-Load API
# This should match the AUTH_KEY configured in your GPT-Load instance
GPTLOAD_AUTH_KEY=your-gptload-auth-key

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# SQLite database location (default is fine for most use cases)
DATABASE_URL=sqlite:///./data/unifiedllm.db

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# Host and port for the UnifiedLLM web interface
APP_HOST=0.0.0.0
APP_PORT=8000

# -----------------------------------------------------------------------------
# DOCKER DEPLOYMENT (Optional - only needed for docker-compose)
# -----------------------------------------------------------------------------
# Port mappings for Docker containers
UNIFIEDLLM_PORT=8000
GPTLOAD_PORT=3001
UNI_API_PORT=8001

# Docker image versions (optional - defaults to latest)
# UNIFIEDLLM_IMAGE=ghcr.io/denniszlei/unifiedllm:latest
# GPTLOAD_IMAGE=ghcr.io/gptload/gptload:latest
# UNI_API_IMAGE=yym68686/uni-api:latest

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS (Optional)
# -----------------------------------------------------------------------------
# GPT-Load URL (use http://gptload:3001 for Docker, http://localhost:3001 for local)
GPTLOAD_URL=http://localhost:3001
